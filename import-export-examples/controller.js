/// DEFAULT EXPORT
const defaultController = {
  message: 'I\'m the Default Controller',
}

export default defaultController;

// EXPORT
const controller = {
  message: 'I\'m the Controller',
}

export { controller };
